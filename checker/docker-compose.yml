version: '3'
services:
  checker-backend:
    build: .
  checker-frontend:
    image: nginx
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - checker-backend
    ports:
      - 4002:80
